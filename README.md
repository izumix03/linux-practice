# Requirements

```shell
brew install lima
```

## Setting
`default.yaml` のマウントディレクトリをプロジェクトの `mount` ディレクトリに変更する

## Launch lima
```shell
limactl start default.yaml
```

## Run
```shell
limactl start default
lima
```